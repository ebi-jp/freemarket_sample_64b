.wrapper-member-info
  .personal-entry
    .signup-page-header
      .personal-entry__header
        .tttttt
          = link_to image_tag('logo_red.svg',{class: "mercarilogo"})
        .progress
          .progress__upper
            .progress_upper_title__long__red 会員情報
            .progress_upper_title__short 電話番号認証
            .progress_upper_title お届け先住所入力
            .progress_upper_title__longer 支払い方法
            .progress_upper_title 完了
          .progress__lower
            .progress_lower_circle__red
            .progress_lower_bar
            .progress_lower_circle
            .progress_lower_bar
            .progress_lower_circle
            .progress_lower_bar
            .progress_lower_circle
            .progress_lower_bar
            .progress_lower_circle
  .info-main
    .info-content
      %h2.info-content__head 会員情報入力
      = form_for @user, url: step2_signup_index_path, method: :get, html: {class: 'forth-main-wrapper__box'} do |f|
        .field.main-content2
          .form-group
            %label{for:"nickname"} ニックネーム
            %span.form-require 必須
            .field-input
              = f.text_field :nickname, placeholder: "例) メルカリ太郎", type: "text", class: "input-default",required:"required"
          .form-group
            %label{for: "email"} メールアドレス
            %span.form-require 必須
            .field-input
              = f.email_field :email, placeholder: "PC・携帯どちらでも可", type: "email", class: "login-input-text",required:"required"
          .form-group
            %label{for:"password"} パスワード
            %span.form-require 必須
            .field-input
              = f.password_field :password, placeholder: "6文字以上", type: "password", class: "login-input-text",required:"required"
          .info ＊英字と数字の両方を含めて設定してください
          %input{name:"注意", type: "checkbox", calss:"checkbox"} パスワードを表示する
          .form-group
            %h3.text-left.sub-head 本人確認
            %p.l-single-text
              安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
          .form-group__name
            .aaa
              .form-group
                %label{for: "family_name"} お名前 (全角)
                %span.form-require 必須
                .field-input
                  = f.text_field :family_name, placeholder: "例) 山田", type: "text", class: "login-input-text-content-left",required:"required"
                  = f.text_field :first_name, placeholder: "例) 彩", type: "text", class: "login-input-text-content-right ",required:"required"
          .form-group__name
            .aaa
              .form-group
                %label{for:"family_name_kana"} お名前カナ (全角)
                %span.form-require 必須
                .field-input
                  = f.text_field :family_name_kana, placeholder: "例) ヤマダ", type: "text", class: "login-input-text-content-left",required:"required"
                  = f.text_field :first_name_kana, placeholder: "例) アヤ", type: "text", class: "login-input-text-content-right",required:"required" 
          .form-group
            %label{for:"birth"} 生年月日
            %span.form-require 必須
            .div
              = f.collection_select :birthyear_id, Birthyear.all, :id, :name, {prompt: "--"}, {class: 'select-default'}
              %a 年
              = f.collection_select :birthmonth_id, Birthmonth.all, :id, :name, {prompt: "--"}, {class: 'select-default'}
              %a 月
              = f.collection_select :birthday_id, Birthday.all, :id, :name, {prompt: "--"}, {class: 'select-default'}
              %a 日

          .form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
          .form-group
          .form-group
            %p.l-single-text.text-center
              「次へ進む」のボタンを押すことにより、
              = link_to "利用規約"
              に同意したものとみなします
          .actions
            = f.submit "次へ進む", class: "btn-red"  
          .links
            = link_to "本人情報の登録について＞", class: "user-link"

